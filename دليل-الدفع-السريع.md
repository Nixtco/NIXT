# 🚀 دليل الإعداد السريع - نظام الدفع SpaceRemit

## ✅ تم! التكامل جاهز

التكامل الكامل لنظام الدفع SpaceRemit موجود بالفعل في المشروع. كل ما تحتاجه هو:

---

## 📝 خطوات الإعداد (5 دقائق فقط!)

### 1️⃣ احصل على مفاتيح SpaceRemit

1. سجل حساب في: https://spaceremit.com
2. اذهب إلى Dashboard → **Websites And Keys**
3. انسخ المفاتيح الأربعة:
   - Public Key (للإنتاج)
   - Secret Key (للإنتاج)
   - Test Public Key (للاختبار)
   - Test Secret Key (للاختبار)

---

### 2️⃣ أضف المفاتيح للـ Frontend

**الملف:** `c:\Users\moham\OneDrive\Desktop\NIXT\.env.local`

```env
NEXT_PUBLIC_SPACEREMIT_PUBLIC_KEY=pk_live_ضع_المفتاح_هنا
NEXT_PUBLIC_SPACEREMIT_TEST_PUBLIC_KEY=pk_test_ضع_المفتاح_هنا
```

---

### 3️⃣ أضف المفاتيح للـ Backend

**الملف:** `c:\Users\moham\OneDrive\Desktop\NixtBackend\.env`

أنشئ الملف إذا لم يكن موجوداً، وانسخ من `.env.example`:

```env
# باقي الإعدادات (Database, Redis, إلخ...)
# ...

# SpaceRemit Payment Gateway
SPACEREMIT_SECRET_KEY=sk_live_ضع_المفتاح_هنا
SPACEREMIT_PUBLIC_KEY=pk_live_ضع_المفتاح_هنا
SPACEREMIT_TEST_SECRET_KEY=sk_test_ضع_المفتاح_هنا
SPACEREMIT_TEST_PUBLIC_KEY=pk_test_ضع_المفتاح_هنا
SPACEREMIT_ENVIRONMENT=development
```

⚠️ **مهم:** عند النشر على الإنتاج، غيّر إلى: `SPACEREMIT_ENVIRONMENT=production`

---

### 4️⃣ أنشئ جدول المدفوعات في قاعدة البيانات

```bash
# من مجلد Backend
cd c:\Users\moham\OneDrive\Desktop\NixtBackend

# شغّل الـ migration
psql -U postgres -d اسم_قاعدة_البيانات -f migrations/2026-07-25-create-payments-table.sql
```

**أو إذا كنت تستخدم pgAdmin:**
1. افتح pgAdmin
2. اتصل بقاعدة البيانات
3. افتح Query Tool
4. انسخ محتوى ملف `migrations/2026-07-25-create-payments-table.sql`
5. شغّل الكود

---

### 5️⃣ أضف Callback URL في SpaceRemit Dashboard

**للإنتاج:**
```
https://yourdomain.com/api/v1/payments/spaceremit/webhook
```

**للتطوير المحلي (استخدم ngrok):**
```bash
# حمّل وشغّل ngrok
ngrok http 3003

# استخدم الرابط الناتج:
https://abc123.ngrok.io/api/v1/payments/spaceremit/webhook
```

---

## 🎯 جرّب الآن!

### شغّل المشروع:

```bash
# Terminal 1 - Frontend
cd c:\Users\moham\OneDrive\Desktop\NIXT
npm run dev

# Terminal 2 - Backend
cd c:\Users\moham\OneDrive\Desktop\NixtBackend
npm run dev
```

### افتح صفحة الدفع:

```
http://localhost:3000/payment?plan=landing&amount=100
```

---

## ✅ تحقق من الإعداد

شغّل هذا الأمر للتحقق من أن كل شيء صحيح:

```bash
cd c:\Users\moham\OneDrive\Desktop\NixtBackend
node scripts/check-spaceremit-setup.js
```

---

## 🎨 كيفية استخدام صفحة الدفع

### طريقة 1: من الكود

```typescript
import { useRouter } from 'next/navigation';

const router = useRouter();

// توجيه للدفع
router.push('/payment?plan=landing&amount=100');
```

### طريقة 2: رابط مباشر

```tsx
<Link href="/payment?plan=landing&amount=100">
  ادفع الآن
</Link>
```

### الخطط المتاحة:

- `landing` - Landing Page (100$)
- `dashboard` - Dashboard (200$)
- `ecommerce` - E-Commerce (500$)
- `custom` - Custom Project (مبلغ مخصص)

---

## 📊 كيفية عرض دفعات المستخدم

```typescript
// في أي component
const fetchPayments = async () => {
  const response = await fetch(
    `${process.env.NEXT_PUBLIC_API_URL}/payments/spaceremit/my-payments`,
    {
      headers: {
        'Authorization': `Bearer ${token}`
      }
    }
  );
  
  const data = await response.json();
  console.log(data.data); // قائمة الدفعات
};
```

---

## 🔒 ميزات الأمان المطبقة

✅ Secret Key محفوظ في Backend فقط  
✅ التحقق المزدوج من الدفع (Frontend + Webhook)  
✅ التحقق من المبلغ والعملة  
✅ حفظ كامل البيانات في قاعدة البيانات  
✅ ربط الدفع بالمستخدم تلقائياً  

---

## 📁 الملفات المهمة

### Frontend:
- `app/payment/page.tsx` - صفحة الدفع الرئيسية
- `components/Payment/SpaceremitCheckout.tsx` - مكون الدفع
- `.env.local` - المفاتيح العامة

### Backend:
- `src/modules/api/v1/restful/routes/payments.routes.ts` - Routes
- `src/modules/api/v1/restful/controllers/payments.controller.ts` - Controllers
- `src/modules/database/postgreSQL/models/Payment.model.ts` - قاعدة البيانات
- `.env` - المفاتيح السرية

---

## 🐛 حل المشاكل

### المشكلة: "Payment gateway is not configured"

**الحل:**
1. تأكد من إضافة المفاتيح في `.env`
2. أعد تشغيل Backend
3. تأكد أن المفاتيح ليست placeholder

### المشكلة: نموذج الدفع لا يظهر

**الحل:**
1. افتح Browser Console (F12)
2. تحقق من عدم وجود أخطاء JavaScript
3. تأكد من اتصالك بالإنترنت (لتحميل SpaceRemit script)

### المشكلة: Webhook لا يعمل

**الحل:**
1. استخدم ngrok للاختبار المحلي
2. تأكد من إضافة الرابط الصحيح في SpaceRemit Dashboard
3. تحقق من Backend logs

---

## 📞 المساعدة

### وثائق شاملة:
- `SPACEREMIT_INTEGRATION_GUIDE.md` - دليل كامل بالعربية
- `docs/PAYMENT_SETUP_CHECKLIST.md` - قائمة تحقق تفصيلية
- `NixtBackend/SPACEREMIT_SETUP.md` - دليل Backend بالإنجليزية

### روابط مفيدة:
- [SpaceRemit Documentation](https://spaceremit.com/api/documentation)
- [SpaceRemit Dashboard](https://spaceremit.com/dashboard)

---

## 🎉 انتهى!

الآن أنت جاهز لاستقبال الدفعات! 

**خطوات سريعة:**
1. ✅ أضف المفاتيح
2. ✅ شغّل Migration
3. ✅ أضف Callback URL
4. ✅ شغّل المشروع
5. ✅ جرّب دفعة تجريبية!

**حظاً موفقاً! 🚀**
